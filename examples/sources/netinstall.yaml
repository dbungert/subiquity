- description:
    en: This version has been customized to have a small runtime footprint in environments
      where humans are not expected to log in.
  id: ubuntu-server-minimal
  locale_support: none
  name:
    en: Ubuntu Server (minimized)
  # where do I get the the version number?
  # could I distinguish daily builds, snapshots, etc
  # where do I download this from?
  path: ubuntu-server-minimal.tar.xz
  # this size will be wrong if the tar.xz changes
  size: 530485248
  type: txz
  variant: server
  location: network
- default: true
  description:
    en: The default install contains a curated set of packages that provide a comfortable
      experience for operating your server.
  id: ubuntu-server
  locale_support: locale-only
  name:
    en: Ubuntu Server
  path: ubuntu-server.tar.xz
  size: 1066115072
  type: txz
  variant: server
  location: network
